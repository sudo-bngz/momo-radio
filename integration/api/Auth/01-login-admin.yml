info:
  name: 01-login-admin
  type: http
  seq: 1

http:
  method: POST
  url: "{{base-url}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
