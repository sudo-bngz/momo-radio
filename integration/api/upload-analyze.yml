info:
  name: upload-analyze
  type: http
  seq: 1

http:
  method: POST
  url: "{{base-url}}/api/v1/upload/analyze"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
