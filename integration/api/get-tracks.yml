info:
  name: get-tracks
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/v1/tracks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
