info:
  name: get-albums
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/api/v1/albums"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
