info:
  name: get-artists
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/v1/artists"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
