info:
  name: create-playlists
  type: http
  seq: 2

http:
  method: POST
  url: "{{base-url}}/api/v1/playlists"
  body:
    type: json
    data: |-
      {
        "name": "Friday Night Techno",
        "color": "#3182ce"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
